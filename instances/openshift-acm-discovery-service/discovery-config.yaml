apiVersion: discovery.open-cluster-management.io/v1
kind: DiscoveryConfig
metadata:
  name: discovery
  namespace: openshift-rhacm-credentials
spec:
  # Must match the RHACM Credential you configured for Red Hat OpenShift Cluster Manager
  credential: rhocm-test
  filters:
    lastActive: 7